//var game;
//var controls;
window.onload = function(){
		var config = {
				type: Phaser.AUTO,
				width: 864,
				height: 608,
				parent: 'phaser-game',
				//scene: [SceneMain]
		};
		var game = new Phaser.Game(config);
}
